# Release is unminified, so these rules are inert today. They are here so that the
# day minifying is turned on, it is a one-line change rather than an
# investigation. gpaper-onyx ships its own consumer rules for the EventBus
# subscriber the palm-rejection gate needs, so nothing is owed to it from here.
