<?xml version="1.0" encoding="utf-8"?>
<!-- Heavier than shape_bordered on purpose: this edge is the only thing telling
     the reader the dialog is in front of the screen rather than part of it. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/paperWhite" />
    <stroke android:width="2dp" android:color="@color/inkBlack" />
    <corners android:radius="4dp" />
</shape>
